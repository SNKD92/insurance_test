const arrayTest = [1, 2, 3];
const somefunc = (a) => (a += 1);
const manualMap = (array, func) => {
  const res = [];
  for (let i = 0; i < array.length; i++) {
    res.push(func(array[i]));
  }
  return res;
};
console.log(manualMap(arrayTest, somefunc));
